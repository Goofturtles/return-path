# Credits

## Photography

Two photographs, both from Pexels under its licence (free to use, no
attribution required). They are downloaded and served from `img/` rather than
hotlinked, because this page's central claim is that it makes no network
requests after load — a third-party image would break that.

- `img/photo-machinery.webp` — Pexels photo 1181320
- `img/photo-night.webp` — Pexels photo 7278805

## Everything else

The sky (`img/cloud-*.webp`) is rendered from `tools/render_clouds.py` in
Blender. The interface images (`img/ui-*.webp`) are screenshots of this tool's
own output. Typefaces are Instrument Serif and IBM Plex Sans/Mono, both SIL
Open Font License, self-hosted in `fonts/`.
